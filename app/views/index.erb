<div class="container">
  <h1>Notes:</h1>
  <ul>
    <% @notes.each do |note| %>
      <li><a href="/notes/<%= note.id %>"><%= note.title %></li>
    <% end %>
  </ul>
  <a href="notes/new">Create a new note</a>
</div>
